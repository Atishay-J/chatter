/**
 *
 * @param {string} serverHistory
 * @param {string} name
 * @param {string} role
 * @param {string} roomName
 */

const createUser = (name, role) => {
  return { name, role };
};

export default createUser;
